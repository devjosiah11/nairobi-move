import { Router } from 'express';
// import { sql } from '@nairobi-move/db';

const router = Router();

// GET /api/routes — all routes with origin/dest stage names
// GET /api/routes/:id — single route with fares
// GET /api/routes/search?origin=&dest= — fuzzy search

export default router;
