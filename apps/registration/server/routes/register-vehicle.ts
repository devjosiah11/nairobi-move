import { Router } from 'express';
// import { sql } from '@nairobi-move/db';

const router = Router();

// POST /api/register/vehicle
// Body: { sacco_id, plate_number, vehicle_type, driver_name, driver_phone,
//         ntsa_expiry, insurance_expiry, psv_expiry }
// Also handles creating a new sacco if sacco_id is not provided

export default router;
