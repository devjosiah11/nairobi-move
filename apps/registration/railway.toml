[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w registration"

[deploy]
startCommand = "npm start -w registration"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
