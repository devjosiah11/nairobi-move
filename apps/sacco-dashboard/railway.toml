[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w sacco-dashboard"

[deploy]
startCommand = "npm start -w sacco-dashboard"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
