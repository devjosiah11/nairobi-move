[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w boda-dispatch"

[deploy]
startCommand = "npm start -w boda-dispatch"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
